return {
  {
    "nvim-treesitter/nvim-treesitter",
    lazy = false,
    -- DELETE or COMMENT OUT the dependencies block for now
    -- dependencies = {
    --   "nvim-treesitter/nvim-treesitter-textobjects",
    -- },
    config = function()
       -- ...
    end,
  },
}